#include "header/Knight.hpp"

Knight::Knight(int* pos, std::string color) {
	setPosition(pos);
	setColor(color);
  setName("Knight");
}

std::vector<std::vector<int>> getPossibleMoves(King ownKing) const {

  // to be implemented I am tired.

}
